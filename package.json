{
  "name": "fluentui-vue",
  "version": "0.0.1",
  "description": "Fluent Design 2 设计规范的 Vue3 实现",
  "keywords": [
    "component library",
    "fluentui",
    "ui",
    "ui framework",
    "vue",
    "vue3"
  ],
  "homepage": "https://fluentui-vue.nahida.website",
  "license": "MIT",
  "author": "夕云葛城",
  "repository": {
    "type": "git",
    "url": "https://github.com/Altermoe/fluentui-vue"
  },
  "scripts": {
    "dev": "pnpm --filter @fluentui-vue/docs dev",
    "docs:dev": "pnpm --filter @fluentui-vue/docs dev",
    "docs:build": "pnpm --filter @fluentui-vue/docs build",
    "docs:generate": "pnpm --filter @fluentui-vue/docs generate",
    "docs:preview": "pnpm --filter @fluentui-vue/docs preview",
    "build": "pnpm -r build",
    "lint": "oxlint --ignore-pattern .dsh --ignore-pattern packages/designs/generated --ignore-pattern packages/designs/data",
    "lint:fix": "oxlint --fix --ignore-pattern .dsh --ignore-pattern packages/designs/generated --ignore-pattern packages/designs/data",
    "format": "oxfmt --check",
    "format:fix": "oxfmt --write",
    "tsc": "pnpm -r tsc",
    "check": "pnpm lint && pnpm format && pnpm tsc"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "oxfmt": "0.60.0",
    "oxlint": "1.75.0",
    "typescript": "^6.0.3",
    "vite": "8.1.5"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.1",
      "onFail": "download"
    }
  }
}
